extends ./layouts/main-layout.pug

block styles
    link(rel="stylesheet", href="/css/user.css")

block content

    h1 Users
    a(href="/register" class="btn-new") Nuevo
    div.users
        each user in users
            div.user
                p.name #{user.nombre}
                p #{user.edad}
                p #{user.genero}
                p #{user.email}                
                a(href="/user/"+user.id class="btn-more") Ver más
            